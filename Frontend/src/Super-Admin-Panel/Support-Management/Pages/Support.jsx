import React from "react";
import SupportInbox from "../../../Shared/Components/SupportInbox";

const Support = () => {
  return (
    <SupportInbox
      title="Super Admin Support Box"
      subtitle="Global visibility for all support requests across the platform, with status tracking and internal notes."
    />
  );
};

export default Support;
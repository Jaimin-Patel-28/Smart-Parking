const VehicleSelector = () => {
  return (
    <section>
      <h2>Select Vehicle</h2>

      <select>
        <option>Select vehicle from profile</option>
      </select>

      <button>Add New Vehicle</button>
    </section>
  );
};

export default VehicleSelector;

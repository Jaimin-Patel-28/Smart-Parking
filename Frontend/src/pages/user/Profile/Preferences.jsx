export const Preferences = () => {
  return (
    <section>
      <h2>Preferences & Settings</h2>
      <select>
        <option>Language</option>
      </select>
      <select>
        <option>Theme Mode</option>
      </select>
      <input type="text" placeholder="Default Parking Location" />
    </section>
  );
};

export default Preferences;
